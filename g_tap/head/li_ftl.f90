!        Generated by TAPENADE     (INRIA, Ecuador team)
!  tapenade 3.x
!
!  Differentiation of li in forward (tangent) mode:
!   variations   of useful results: li
!   with respect to varying inputs: *temp *propunit *tsal
!   Plus diff mem management of: temp:in propunit:in tsal:in pres:in
!>    @brief average thermal conductivities on cell faces in x direction
!>    @param[in] i grid indices
!>    @param[in] j grid indices
!>    @param[in] k grid indices
!>    @param[in] ismpl local sample index
!>    @return x thermal conductivity (J/mK)
DOUBLE PRECISION FUNCTION g_LI(i, j, k, ismpl, li)
  USE ARRAYS

  USE g_ARRAYS

  USE MOD_GENRL
  USE MOD_TEMP
  IMPLICIT NONE
  INTEGER :: ismpl
  INTEGER :: i, j, k
  EXTERNAL LX
  EXTERNAL g_LX
  DOUBLE PRECISION :: f1, f2, prod, summ, LX
  DOUBLE PRECISION :: g_f1, g_f2, g_prod, g_summ, g_LX
  DOUBLE PRECISION :: li
  li = 0.d0
  IF (i0 .GT. 1 .AND. i .LT. i0) THEN
    g_f1 = g_LX(i, j, k, ismpl, f1)
    g_f2 = g_LX(i + 1, j, k, ismpl, f2)
    g_prod = f2*g_f1 + f1*g_f2
    prod = f1*f2
    g_summ = delx(i+1)*g_f1 + delx(i)*g_f2
    summ = f1*delx(i+1) + f2*delx(i)
    IF (summ .GT. 0.d0) THEN
      g_li = 2.d0*(g_prod-prod*g_summ/summ)/summ
      li = 2.d0*prod/summ
    ELSE
      g_li = 0.D0
    END IF
  ELSE
    g_li = 0.D0
  END IF
!       write(99,'(a,5G14.5)') 'in li ',f1,f2,prod,summ,li
  RETURN
END FUNCTION g_LI

