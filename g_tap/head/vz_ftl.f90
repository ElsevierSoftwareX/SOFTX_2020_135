!        Generated by TAPENADE     (INRIA, Ecuador team)
!  tapenade 3.x
!
!  Differentiation of vz in forward (tangent) mode:
!   variations   of useful results: vz
!   with respect to varying inputs: *temp *head *propunit *tsal
!                *pres
!   Plus diff mem management of: temp:in head:in propunit:in tsal:in
!                pres:in
!>    @brief calculate velocities at cell  faces
!>    @param[in] i grid indices
!>    @param[in] j grid indices
!>    @param[in] k grid indices
!>    @param[in] ismpl local sample index
!>    @return z velocity (m/s)
DOUBLE PRECISION FUNCTION g_VZ(i, j, k, ismpl, vz)
  USE ARRAYS

  USE g_ARRAYS

  USE MOD_GENRL
  IMPLICIT NONE
  INTEGER :: ismpl
  INTEGER :: i, j, k
  EXTERNAL TK, BUOY
  EXTERNAL g_TK, g_BUOY
  DOUBLE PRECISION :: dif, TK, BUOY
  DOUBLE PRECISION :: g_dif, g_TK, g_BUOY
  EXTERNAL DUMMY
  DOUBLE PRECISION :: result1
  DOUBLE PRECISION :: g_result1
  DOUBLE PRECISION :: result2
  DOUBLE PRECISION :: g_result2
  DOUBLE PRECISION :: vz
  vz = 0.d0
  IF (.NOT.head_active .AND. vdefaultswitch) vz = vdefault(3, ismpl)
  IF (k0 .GT. 1 .AND. k .LT. k0 .AND. head_active) THEN
#ifdef head_base
    g_dif = g_head(i, j, k+1, ismpl) - g_head(i, j, k, ismpl)
    dif = head(i, j, k+1, ismpl) - head(i, j, k, ismpl)
    g_result1 = g_TK(i, j, k, ismpl, result1)
    g_result2 = g_BUOY(i, j, k, ismpl, result2)
    g_vz = -(dif*g_result1) - result1*g_dif - g_result2
    vz = -(result1*dif) - result2
#endif
  ELSE
    g_vz = 0.D0
  END IF
  RETURN
END FUNCTION g_VZ

